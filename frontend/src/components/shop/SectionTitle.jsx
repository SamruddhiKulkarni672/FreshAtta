// components/shop/SectionTitle.jsx
import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <h1 className="text-2xl font-bold mb-6 text-center text-gray-800">
      {title}
    </h1>
  );
};

export default SectionTitle;
